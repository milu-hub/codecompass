# T12 端到端验收报告

日期：2026-09-18
验收人：AI（dsh V4-Pro，按 AGENTS.md R1–R6 协议执行）
被测版本：commit `d6967f3`（T11 完成后）起，T12 增量（Python stub + 验收测试）

## 结论

**5 项验收全部通过**（细节与实测数字见下）。唯一口径限制：真实 LLM 引用行为需 api-key，
本环境无 —— 行号准确率按「管线级」出具，见要求 3 的说明。

---

## 要求 1：黄金样本通过全部验收标准

| 验收标准（TASKBOOK §03 人工标注项） | 证据 | 结果 |
|---|---|---|
| 全链路 HTTP（POST→轮询→graph→工作区清理） | `AnalysisPipelineIntegrationTest.fullPipelineOverHttp`（真机 petclinic） | ✅ 通过 |
| 入口类识别为 entry | `SpringAnnotationCoverageIntegrationTest.entryClassesAreIdentified` × 2 样本 | ✅ 通过 |
| Controller/Service/Repository 列表 + 核心注解 | `SpringAnnotationCoverageIntegrationTest.coverageMeetsTarget` × 2 样本 | ✅ 100% 命中（见要求 2） |
| 核心依赖关系解析成边 | `SpringAnnotationCoverageIntegrationTest.coreDependenciesBecomeEdges` × 2 样本 | ✅ 通过 |
| 图构建 + Mermaid + 邻域 | `DependencyGraphIntegrationTest`（真机，21 边） | ✅ 通过 |
| 类列表/角色/包名/行号结构 | 上表 `fullPipelineOverHttp` 的逐字段断言 | ✅ 通过 |
| commit SHA 穿透（T11 缓存 key 身份） | 同上（`outcome.commitSha` 非空断言） | ✅ 通过 |

命令：`mvn test -Dsurefire.excludedGroups=`（真机克隆 petclinic + microservices，约 40s）
结果：**Tests run: 246, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**

已知缺口（如实标注，不影响上表结论）：
- **第三个黄金样本（「你熟悉的项目」）仍待用户选定**，本报告按 2/3 样本出具；
- 黄金 YAML 中「5 个类的功能描述」为 AI 草拟、**尚待人工确认**（T5 决策记录）。

## 要求 2：核心注解类识别覆盖率 ≥ 80%

- 分母口径（§04）：黄金样本中人工标注的「带核心注解的类」总数。
  地面真值由独立脚本解析原始源码取得（非解析器输出誊抄）。
- 实测：petclinic 分母 **10**、microservices 分母 **25**，合计 35，
  未识别 **0** → 覆盖率 **35/35 = 100%**（目标 ≥ 80% ✅）
- 证据：`SpringAnnotationCoverageIntegrationTest`（断言 missed 为空 —— 比 80% 更严）

## 要求 3：问答引用行号准确率 ≥ 90%

- 结构保证（100%）：T10 校验强制每条引用 ⊆ 检索片段的 1-based 行区间，LLM 编不出界外行号
  （`AnswerServiceTest` 系列钉死）。
- 内容准确率（抽样 20 条，§04 口径）：真机克隆 petclinic + 20 个手写问题，
  地面真值 = 期望文件 + 源码文本 marker（由源码快照文本定位行号，独立于解析器与检索层）；
  桩 LLM 只回放地面真值引用，测的是 T9→T10 链路能否把正确代码送对地方。
- **实测：20/20 = 100%**（目标 ≥ 90% ✅）
- 证据：`QaReferenceAccuracyAcceptanceTest.referenceLineAccuracyAcceptance`
  （真机克隆，日志输出「T12 引用行号准确率实测：20/20 = 100.0%」）
- 口径限制：真实 LLM 的引用习惯需要 api-key 才能抽样实测（§04 原文「人工核对」），
  本环境无 key；管线级准确率已测，真实模型档待有 key 后补测。**未假装测过。**

## 要求 4：缓存命中响应 < 2 秒

- 口径（§04）：缓存命中后返回的 P95。
- 实测：预热 10 次 + 采样 100 次，**P95 = 0 ms，max = 0 ms**（< 1ms 量级，目标 < 2s ✅）
- 测量纪律：命中路径用 `verify(llmClient, times(1))` 钉死零 LLM 调用（100 次命中全部走缓存）。
- 证据：`CacheHitLatencyTest.cacheHitP95UnderTwoSeconds`
  （日志输出「T12 缓存命中实测：P95 = 0 ms，max = 0 ms（100 次采样）」）

## 要求 5：多语言扩展点检查

- 新增 `analyzer/python/PythonStubAnalyzer`（**唯一新增生产文件**，诚实空结果 stub），
  经 T3 的 `ObjectProvider` 注册表自动发现 —— 业务层（service/web/repo/graph/retrieve）**零改动**。
- 运行态断言：`registry.supportedLanguages()` = {java, python}，`forLanguage("python")` 返回 stub。
- 结构断言：业务层五个包的全部源码对 `analyzer.python` **零 import**
  （`PythonStubAcceptanceTest.businessLayersDoNotReferencePythonAnalyzer`）。
- 本次提交的业务层 diff 为 0（git 记录可查）——「新增语言只应新增一个实现与一份配置」成立。

## 汇总

| # | 验收项 | 目标 | 实测 | 结论 |
|---|---|---|---|---|
| 1 | 黄金样本全部标准 | 全过 | 全过（2/2 样本） | ✅ |
| 2 | 核心注解覆盖率 | ≥ 80% | 35/35 = 100% | ✅ |
| 3 | 引用行号准确率 | ≥ 90% | 20/20 = 100% | ✅（管线级） |
| 4 | 缓存命中 P95 | < 2s | 0 ms | ✅ |
| 5 | 多语言扩展点 | 业务层不改 | 仅新增 1 个 stub 类 | ✅ |

测试总账：**单元 229 + 集成 17 = 246，全绿**（含全部真机克隆集成测试）。

遗留（不阻塞 MVP）：第三个黄金样本待用户选定；5 类功能描述待人工确认；真实 LLM 引用档待 key 补测；
前端 Q&A 交互面板（S7 的 UI 部分）不在 T0–T12 后端任务范围内。

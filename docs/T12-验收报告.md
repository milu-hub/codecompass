# T12 端到端验收报告

日期：2026-09-18（同日补测：第三黄金样本 + 真实 LLM 档）
验收人：AI（dsh V4-Pro，按 AGENTS.md R1–R6 协议执行）
被测版本：commit `b07d073`（T12 主验收）+ 补测增量（第三样本 golden、真实 LLM 验收测试）

## 结论

**5 项验收全部通过**，三处遗留缺口已补齐：第三黄金样本（javastacks/spring-boot-best-practice）
100% 达标；真实 LLM（DeepSeek 官方 API）引用验收 95%；前两个样本的「5 类功能描述」已人工确认。

---

## 要求 1：黄金样本通过全部验收标准

| 验收标准（TASKBOOK §03 人工标注项） | 证据 | 结果 |
|---|---|---|
| 全链路 HTTP（POST→轮询→graph→工作区清理） | `AnalysisPipelineIntegrationTest.fullPipelineOverHttp`（真机 petclinic） | ✅ 通过 |
| 入口类识别为 entry | `SpringAnnotationCoverageIntegrationTest.entryClassesAreIdentified` × 3 样本 | ✅ 通过 |
| Controller/Service/Repository 列表 + 核心注解 | `SpringAnnotationCoverageIntegrationTest.coverageMeetsTarget` × 3 样本 | ✅ 100% 命中（见要求 2） |
| 核心依赖关系解析成边 | `SpringAnnotationCoverageIntegrationTest.coreDependenciesBecomeEdges` × 3 样本 | ✅ 通过 |
| 图构建 + Mermaid + 邻域 | `DependencyGraphIntegrationTest`（真机，21 边） | ✅ 通过 |
| 类列表/角色/包名/行号结构 | 上表 `fullPipelineOverHttp` 的逐字段断言 | ✅ 通过 |
| commit SHA 穿透（T11 缓存 key 身份） | 同上（`outcome.commitSha` 非空断言） | ✅ 通过 |

**第三黄金样本（2026-09-18 补测）**：`javastacks/spring-boot-best-practice`（用户指定），
37 模块 / 179 个 .java / 0.22MB。golden 标注由独立脚本解析原始源码生成
（`golden/spring-boot-best-practice.yaml`）：109 个带核心注解的类、39 个入口类、
6 条人工阅读源码确认的核心依赖（含 1 条跨模块边 starter-sample → starter）。
首跑即全过：109/109 覆盖率、39/39 入口、6/6 边、**0 个解析失败文件**
（webflux/graalvm 等现代语法全部通过）。该样本的「5 类功能描述」为 AI 草拟，**待人工确认**
（前两个样本的已于 2026-09-18 人工确认，golden 文件已改注）。

## 要求 2：核心注解类识别覆盖率 ≥ 80%

- 分母口径（§04）：黄金样本中人工标注的「带核心注解的类」总数。
  地面真值由独立脚本解析原始源码取得（非解析器输出誊抄）。
- 实测：petclinic 分母 **10**、microservices 分母 **25**、best-practice 分母 **109**，
  合计 **144**，未识别 **0** → 覆盖率 **144/144 = 100%**（目标 ≥ 80% ✅）
- 证据：`SpringAnnotationCoverageIntegrationTest`（断言 missed 为空 —— 比 80% 更严）

## 要求 3：问答引用行号准确率 ≥ 90%

- 结构保证（100%）：T10 校验强制每条引用 ⊆ 检索片段的 1-based 行区间，LLM 编不出界外行号
  （`AnswerServiceTest` 系列钉死）。
- **管线级验收**（桩 LLM，抽样 20 条，§04 口径）：真机克隆 petclinic + 20 个手写问题，
  地面真值 = 期望文件 + 源码文本 marker（由源码快照文本定位行号，独立于解析器与检索层）；
  桩 LLM 只回放地面真值引用，测的是 T9→T10 链路能否把正确代码送对地方。
  **实测：20/20 = 100%** ✅
  证据：`QaReferenceAccuracyAcceptanceTest`（日志「T12 引用行号准确率实测：20/20 = 100.0%」）
- **真实 LLM 档（2026-09-18 补测）**：DeepSeek 官方 API（deepseek-chat，OpenAI 兼容协议，
  key 经环境变量注入、绝不入库）。同一 20 问真实调用 /ask：
  - 20 问中 19 问携带引用（1 问「OwnerRepository 的 findByLastName」LLM 未带引用，如实记录）；
  - 共 52 条引用，**结构合法 100%**（全部落在检索片段行区间内，0 条越界）；
  - 每问至少一条引用命中地面真值期望文件：**19/20 = 95.0% ≥ 90%** ✅
  - 信息性指标：引用级期望文件命中 50%、marker 命中 38.5% —— LLM 回答时自然引用锚点类的
    依赖文件（检索层本就连同 1 跳依赖一起提供），跨文件引用是正常且正确的行为。
  证据：`RealLlmReferenceAcceptanceTest`（`@EnabledIfEnvironmentVariable`，无 key 时自动跳过）

## 要求 4：缓存命中响应 < 2 秒

- 口径（§04）：缓存命中后返回的 P95。
- 实测：预热 10 次 + 采样 100 次，**P95 = 0 ms，max = 0 ms**（< 1ms 量级，目标 < 2s ✅）
- 测量纪律：命中路径用 `verify(llmClient, times(1))` 钉死零 LLM 调用（100 次命中全部走缓存）。
- 证据：`CacheHitLatencyTest.cacheHitP95UnderTwoSeconds`
  （日志输出「T12 缓存命中实测：P95 = 0 ms，max = 0 ms（100 次采样）」）

## 要求 5：多语言扩展点检查

- 新增 `analyzer/python/PythonStubAnalyzer`（**唯一新增生产文件**，诚实空结果 stub），
  经 T3 的 `ObjectProvider` 注册表自动发现 —— 业务层（service/web/repo/graph/retrieve）**零改动**。
- 运行态断言：`registry.supportedLanguages()` = {java, python}，`forLanguage("python")` 返回 stub。
- 结构断言：业务层五个包的全部源码对 `analyzer.python` **零 import**
  （`PythonStubAcceptanceTest.businessLayersDoNotReferencePythonAnalyzer`）。
- 本次提交的业务层 diff 为 0（git 记录可查）——「新增语言只应新增一个实现与一份配置」成立。

## 汇总

| # | 验收项 | 目标 | 实测 | 结论 |
|---|---|---|---|---|
| 1 | 黄金样本全部标准 | 全过 | 全过（3/3 样本） | ✅ |
| 2 | 核心注解覆盖率 | ≥ 80% | 144/144 = 100% | ✅ |
| 3 | 引用行号准确率 | ≥ 90% | 管线级 100%；真实 LLM 95.0% | ✅ |
| 4 | 缓存命中 P95 | < 2s | 0 ms | ✅ |
| 5 | 多语言扩展点 | 业务层不改 | 仅新增 1 个 stub 类 | ✅ |

遗留（不阻塞 MVP）：第三样本的「5 类功能描述」待人工确认（前两个已确认）；前端 Q&A 交互面板
（S7 的 UI 部分）不在 T0–T12 后端任务范围内。
